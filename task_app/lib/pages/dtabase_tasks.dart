class Task {
  final String title;
  //Aqui abajo antes de bool estaba un "final"
  bool done;
  final String description;
  Task({required this.title, required this.done, required this.description});
}
